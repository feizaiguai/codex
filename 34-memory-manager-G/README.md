# Memory Manager - 用户记忆管理器

**版本**: 1.0.0  
**状态**: 生产就绪 ✅  
**质量**: A+ (生产级)  
**语言**: 简体中文 🇨🇳

---

## 🎯 快速开始

### 什么是 Memory Manager？

Memory Manager 是**用户记忆管理工具**，用于管理 Claude Code 的用户偏好和配置。

它可以帮你：
- 💾 **持久化存储**用户偏好（如"所有输出使用简体中文"）
- ⚙️ **自定义配置** Claude Code 的行为
- 📋 **查看和管理**所有保存的偏好设置

---

## 🚀 使用方法

### 在 Claude Code 中

只需输入：

```bash
/memory [操作] [参数]
```

**示例**：

```bash
# 设置语言偏好
/memory set language.output "简体中文"

# 查看所有偏好
/memory show

# 获取特定偏好
/memory get language.output
```

---

## 📖 常用命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/memory show` | 显示所有用户偏好 | 查看所有已保存的设置 |
| `/memory get <key>` | 获取特定偏好 | `/memory get language.output` |
| `/memory set <key> <value>` | 设置偏好 | `/memory set language.output "简体中文"` |
| `/memory delete <key>` | 删除偏好 | `/memory delete old_setting` |
| `/memory path` | 显示配置文件路径 | 查看 settings.json 位置 |

---

## 💡 使用示例

### 示例 1：设置语言为简体中文

```bash
/memory set language.output "简体中文"
/memory set language.preference "所有发给用户的文字都使用简体中文"
```

**效果**：
- ✅ Claude 会记住你的语言偏好
- ✅ 未来所有输出都将使用简体中文
- ✅ 设置持久化，重启后仍然生效

### 示例 2：查看当前设置

```bash
/memory show
```

**输出**：
```json
{
  "language": {
    "output": "简体中文",
    "preference": "所有发给用户的文字都使用简体中文"
  }
}
```

### 示例 3：设置工作偏好

```bash
# 设置代码风格
/memory set coding.style "google"

# 设置默认缩进
/memory set coding.indentation 2

# 设置是否总是包含类型提示
/memory set coding.typeHints true
```

---

## 🔧 高级功能

### 1. 嵌套配置

使用点号（`.`）创建层级结构：

```bash
/memory set project.python.version "3.11"
/memory set project.python.packageManager "poetry"
/memory set project.node.version "20"
```

### 2. JSON 值

设置复杂对象或数组：

```bash
# 设置对象
/memory set myConfig '{"theme": "dark", "fontSize": 14}'

# 设置数组
/memory set favoriteLanguages '["Python", "TypeScript", "Rust"]'
```

### 3. 自动备份

每次修改时自动备份到 `settings.json.backup`，确保安全。

---

## 📁 配置文件位置

**Windows**: `C:\Users\<用户名>\.claude\settings.json`  
**macOS/Linux**: `~/.claude/settings.json`

查看路径：
```bash
/memory path
```

---

## 🎓 最佳实践

### 1. 语言偏好设置

```bash
# 推荐：明确指定语言偏好
/memory set language.output "简体中文"
/memory set language.docs "简体中文"
/memory set language.codeComments "简体中文"
```

### 2. 使用有意义的键名

✅ **好**：`language.output`, `coding.style`, `project.template`  
❌ **差**：`lang`, `style`, `tmpl`

### 3. 组织相关配置

```bash
# 语言相关
/memory set language.output "简体中文"
/memory set language.preference "所有输出简体中文"

# 编码相关
/memory set coding.style "google"
/memory set coding.indentation 2
```

---

## 🔒 安全提示

⚠️ **不要存储敏感信息**：
- ❌ 不要存储 API keys、密码
- ❌ 不要存储个人身份信息
- ✅ 仅存储偏好和配置

---

## 🆘 常见问题

### Q: 设置后为什么没有立即生效？

A: 某些设置可能需要重启 Claude Code 才能生效。对于用户偏好（如语言设置），应该在下一次对话中生效。

### Q: 如何重置所有设置？

A: 删除或重命名 `~/.claude/settings.json` 文件，Claude Code 会重新创建默认配置。

### Q: 设置会同步到其他设备吗？

A: 不会。`settings.json` 是本地文件，不会自动同步。如需同步，可以手动复制文件或使用云存储。

### Q: 我可以编辑 settings.json 文件吗？

A: 可以，但推荐使用 Memory Manager 工具，它会自动备份并验证 JSON 格式。

---

## 🌟 总结

**Memory Manager** = 简单、安全、高效的用户偏好管理

✅ 持久化存储用户偏好  
✅ 支持嵌套配置和 JSON 值  
✅ 自动备份，数据安全  
✅ 简体中文输出  
✅ 易于使用

**质量**：生产级  
**状态**：✅ 生产就绪  
**语言**：简体中文 🇨🇳

---

**位置**: `C:\Users\bigbao\.claude\skills\35-memory-manager\`  
**调用**: `/memory [操作] [参数]`  
**版本**: 1.0.0
